<!DOCTYPE html>
<html>
<head>
  <title>JobHunt</title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <nav class="navbar sticky-top nav-style">
    <%= image_tag 'logo.png', class: "logo-img" %>
    <% if current_user %>
    <ul class="navbar-right list-inline">
      <li class="list-inline-item"><%= link_to "Dashboard", dashboard_index_path, class: 'text-muted' %></li>
      <li class="list-inline-item"><%= link_to "Logout", logout_path, class: 'text-muted', method: :delete %></li>
    </ul>
    <% end %>
  </nav>
  <% flash.each do |name, msg| %>
  <div class="transparent-white"><%= content_tag :div, sanitize(msg), class: name %></div>
  <% end %>
  <%= yield %>
</body>
</html>
